    # Controle de Despesas

Instuções rápidas:

## Backend

cd backend
cp .env.example .env
npm install
npm run dev

## Frontend

Abra frontend/index.html no navegador (ou use um servidor estático).

O backend espera MongoDB em MONGODB_URI (padrão: mongodb://localhost:27017/expenses).
